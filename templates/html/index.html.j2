<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Serveur Nginx configuré par Ansible</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap');

        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #2C3E50, #4A6E8A);
            color: #ECF0F1;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            text-align: center;
        }

        .container {
            background-color: rgba(44, 62, 80, 0.8);
            padding: 40px 60px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            border: 1px solid #7F8C8D;
        }

        h1 {
            color: #3498DB;
            font-size: 3em;
            margin-bottom: 20px;
            font-weight: 700;
        }

        p {
            font-size: 1.2em;
            line-height: 1.6;
            margin-bottom: 10px;
        }

        .info {
            background-color: #2C3E50;
            border-radius: 8px;
            padding: 15px;
            margin-top: 25px;
            border-left: 5px solid #3498DB;
            text-align: left;
        }

        .info strong {
            color: #3498DB;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🚀 Nginx est en ligne !</h1>
        <p>Ce serveur web a été déployé et configuré automatiquement avec Ansible.</p>
        
        <div class="info">
            <p><strong>Nom d'hôte du serveur :</strong> {{ ansible_hostname }}</p>
            <p><strong>Adresse IP :</strong> {{ ansible_default_ipv4.address }}</p>
            <p><strong>Distribution OS :</strong> {{ ansible_distribution }} {{ ansible_distribution_version }}</p>
        </div>
    </div>
</body>
</html> 